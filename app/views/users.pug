doctype html
html
  head
    title Users
  body
    h1 Users List

    if users.length === 0
      p No users found.
    else
      ul
        each user in users
          li
            a(href=`/users/${user.id}`)= user.name
            