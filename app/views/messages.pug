//- # MESSAGES PAGE
doctype html
html
  head
    title Messages
    link(rel="stylesheet", href="/style.css")

  body
    h1 💬 Messages

    //- # SEND MESSAGE FORM
    .card
      h2 Send a Message

      form(method="POST" action="/messages")
        p
          label(for="sender_id") From:
          br
          select(name="sender_id" required)
            option(value="1") Ilhan Mohamed
            option(value="2") Sabrin Saed
            option(value="3") Huda Abi
            option(value="4") Amina Aden

        p
          label(for="receiver_id") To:
          br
          select(name="receiver_id" required)
            option(value="1") Ilhan Mohamed
            option(value="2") Sabrin Saed
            option(value="3") Huda Abi
            option(value="4") Amina Aden

        p
          label(for="message_text") Message:
          br
          textarea(name="message_text" rows="4" cols="40" required)

        br
        button(type="submit") Send Message

    //- # SHOW ALL MESSAGES
    each message in messages
      .card
        p 💌 #{message.sender_name} → #{message.receiver_name}
        p "#{message.message_text}"

    br
    a(href="/") ⬅ Back Home

    




